import React from 'react';
import ReactDOM from 'react-dom/client';
import MyForm from './components/AppForm';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <MyForm />
    </React.StrictMode>
);
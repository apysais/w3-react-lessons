import React from 'react';
import ReactDOM from 'react-dom/client';
import MyForm from './components/AppFormInputs';

const root = document.getElementById('root');
ReactDOM.createRoot(root).render(
    <React.StrictMode>
        <MyForm />
    </React.StrictMode>
);

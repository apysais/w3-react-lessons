import React from 'react';
import ReactDOM from 'react-dom/client';
import Garage from './components/AppList';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<Garage />);